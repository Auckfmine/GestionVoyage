<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
</head>
<body>

<table class="table">
    <thead>
    <tr>
        <th>Id</th>
        <th>Ref_voyage</th>
        <th>Station_Depart</th>
        <th>Station_Arrive</th>
        <th>Vehicule</th>
        <th>Ligne</th>

        <th>Date_depart</th>
        <th>Date_arrive</th>
        <th>actions</th>
    </tr>
    </thead>
    <tbody>
    {% for voyagee in voyages %}
        <tr>
            <td>{{ voyagee.id }}</td>
            <td>{{ voyagee.refVoyage }}</td>
            <td>{{ voyagee.getStationDepart()}}</td>
            <td>{{ voyagee.getStationArrive()}}</td>
            <td>{{ voyagee.MoyenDeTransport }}</td>
            <td>{{ voyagee.ligne ? voyage.eligne|join(', ') : '' }}</td>
            <td>{{ voyagee.dateDepart ? voyagee.dateDepart|date('Y-m-d H:i:s') : '' }}</td>
            <td>{{ voyagee.dateArrive ? voyagee.dateArrive|date('Y-m-d H:i:s') : '' }}</td>
            <td>
                <a class="btn btn-primary" href="{{ path('voyage_show', {'id': voyagee.id}) }}">show</a>
                <a class="btn btn-danger" href="{{ path('voyage_edit', {'id': voyagee.id}) }}">edit</a>
            </td>
        </tr>
        <div class="card" style="width: 18rem;">
            <div class="card-body">
                <h5 class="card-title">{{ voyagee.id }}</h5>
                <h6 class="card-subtitle mb-2 text-muted">{{ voyagee.getStationDepart() }} --> {{ voyagee.getStationArrive() }}</h6>
                <p class="card-text">Some quick example text to build on the card title and make up the bulk of the card's content.</p>
                <a href="#" class="card-link">Card link</a>
                <a href="#" class="card-link">Another link</a>
            </div>
        </div>
    {% else %}
        <tr>
            <td colspan="6">no records found</td>
        </tr>
    {% endfor %}


    </tbody>
</table>

</body>
</html>